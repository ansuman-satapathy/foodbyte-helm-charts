apiVersion: v1
kind: Service
metadata:
  name: {{ include "foodbyte-user-service.fullname" . }}
  labels:
    {{- include "foodbyte-user-service.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "foodbyte-user-service.selectorLabels" . | nindent 4 }}
