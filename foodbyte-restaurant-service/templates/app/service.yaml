apiVersion: v1
kind: Service
metadata:
  name: {{ include "foodbyte-restaurant-service.fullname" . }}
  labels:
    {{- include "foodbyte-restaurant-service.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "foodbyte-restaurant-service.selectorLabels" . | nindent 4 }}
