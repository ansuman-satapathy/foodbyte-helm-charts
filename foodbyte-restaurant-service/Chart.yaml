apiVersion: v2
name: foodbyte-restaurant-service
description: A Helm chart for the foodbyte restaurant service

type: application

version: 0.1.0

appVersion: "1.0.0"

dependencies:
  - name: mongodb
    version: 18.x.x
    repository: "https://charts.bitnami.com/bitnami"
    condition: mongodb.enabled
