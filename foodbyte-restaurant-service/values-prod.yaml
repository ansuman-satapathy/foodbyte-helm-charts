# Production Overrides for AWS EKS (Full Self-Hosted)
replicaCount: 2

useSecrets: true

externalDatabase:
  enabled: false

imagePullSecrets:
  - name: ghcr-pull-secret

resources:
  limits:
    cpu: 500m
    memory: 512Mi
  requests:
    cpu: 250m
    memory: 256Mi
