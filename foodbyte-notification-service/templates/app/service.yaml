apiVersion: v1
kind: Service
metadata:
  name: {{ include "foodbyte-notification-service.fullname" . }}
  labels:
    {{- include "foodbyte-notification-service.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "foodbyte-notification-service.selectorLabels" . | nindent 4 }}
